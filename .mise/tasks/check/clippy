#!/bin/sh
#MISE description="Run clippy with warnings denied"
#MISE depends=["rust:bootstrap"]
set -eu

cd "${MISE_CONFIG_ROOT:-$PWD}"
exec cargo clippy --all-targets --all-features -- -D warnings "$@"
